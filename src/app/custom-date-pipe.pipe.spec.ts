import { CustomDatePipePipe } from './customDate.pipe';

describe('CustomDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
